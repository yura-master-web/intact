/*
console.log('modal');
window.REMODAL_GLOBALS = {
	NAMESPACE: 'modal',
	DEFAULTS: {
		hashTracking: false
	}
};
*/
/*
// Что бы в обход всего открыть модальное окно
let instModal;
$(() => {
	$('[data-remodal-id="modal"]').each((i, el) => {
		instModal = $(el).remodal();

		// console.log(instModal);
		instModal.open();
	});
});
*/
