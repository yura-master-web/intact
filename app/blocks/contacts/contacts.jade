mixin contacts()
	+b.contacts.row.justify-center&attributes(attributes)
		+e('h2').title.block-title_trast-us Контакты

		+b('p').block-title-sub Если у вас остались вопросы, пожалуйста заполните форму

		+b('form').contact-form.bdrs.justify-center.cl-form.js-contact-form

			+b('label').form-input
				+e('input').item.bdrs(
					name="name"
					placeholder="Ваше имя"
					required
				)

			+b('label').form-input
				+e('input').item.bdrs(
					type="email"
					name="email"
					placeholder="Контактный email"
					required
				)

			+b('label').form-input
				+e('input').item.bdrs(
					name="phone"
					placeholder="Номер телефона"
					required
				)

			+b('label').form-input_textarea
				+e('textarea').item.bdrs(
					name="message"
					placeholder="Сообщение"
					rows="3"
				)

			+e.form-btn
				+e('button').btn-sub.button.btn-hover.bg-secondary-color.tdn.upcs.bdrs.js-btn-form-submit Оставить заявку

				+b('label').form-agree.pos-rel
					+e('input').check.visuallyhidden(type="checkbox" name="agree" required)
					+e('p').txt.pos-rel Согласен с политикой конфиденциальности
